import { AgeFilterPipe } from './age-filter.pipe';

describe('AgeFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new AgeFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
